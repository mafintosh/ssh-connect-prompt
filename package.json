{
  "name": "ssh-connect",
  "description": "Connect to ssh using ssh2 with support for interactive events, known_hosts, defaults private keys and agents",
  "version": "0.0.1",
  "dependencies": {
    "once": "~1.3.0",
    "read": "~1.0.5",
    "ssh2": "0.2.16"
  }
}
